<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>打地鼠小遊戲</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f0f0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        h1 { color: #333; margin-bottom: 10px; }

        .game-info {
            font-size: 24px;
            margin-bottom: 20px;
        }

        /* 遊戲地圖：3x3 網格 */
        .game-board {
            display: grid;
            grid-template-columns: repeat(3, 120px);
            grid-template-rows: repeat(3, 120px);
            gap: 15px;
            background-color: #8b4513;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        /* 地洞 */
        .hole {
            background-color: #4d2600;
            border-radius: 50%;
            position: relative;
            overflow: hidden;
            border: 4px solid #331a00;
        }

        /* 地鼠 */
        .mole {
            width: 80%;
            height: 80%;
            background-color: #a0522d;
            border-radius: 50% 50% 20% 20%;
            position: absolute;
            bottom: -100%; /* 初始隱藏在洞下 */
            left: 10%;
            transition: bottom 0.1s ease;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
        }

        /* 地鼠出現的樣式 */
        .mole.up {
            bottom: 5%;
        }

        button {
            margin-top: 30px;
            padding: 10px 25px;
            font-size: 18px;
            cursor: pointer;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            transition: 0.3s;
        }

        button:hover { background-color: #45a049; }
    </style>
</head>
<body>

    <h1>打地鼠！</h1>
    <div class="game-info">
        分數: <span id="score">0</span> | 剩餘時間: <span id="time">20</span>s
    </div>

    <div class="game-board">
        <div class="hole"><div class="mole">🐹</div></div>
        <div class="hole"><div class="mole">🐹</div></div>
        <div class="hole"><div class="mole">🐹</div></div>
        <div class="hole"><div class="mole">🐹</div></div>
        <div class="hole"><div class="mole">🐹</div></div>
        <div class="hole"><div class="mole">🐹</div></div>
        <div class="hole"><div class="mole">🐹</div></div>
        <div class="hole"><div class